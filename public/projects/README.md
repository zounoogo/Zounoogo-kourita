# Vidéos de démo

Dépose ici tes fichiers `.mp4` / `.webm`, puis référence-les dans
`src/data/projects.ts` :

```ts
videoUrl: "/projects/mon-fichier.mp4"
```

Garde les fichiers légers (~10–15 Mo max). Au-delà, préfère un lien
YouTube / Vimeo dans `videoUrl`.
